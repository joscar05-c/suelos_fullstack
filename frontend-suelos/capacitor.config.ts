import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // appId: 'io.ionic.starter',
  appId: 'com.suelos.app',
  appName: 'frontend-suelos',
  webDir: 'www'
};

export default config;
